=== ИНСТРУКЦИЯ ПО ОБНОВЛЕНИЮ РЕПОЗИТОРИЯ ===

Все файлы для обновления находятся в этом архиве.
Выполни команды по порядку:

1. Перейди в папку с клонированным репозиторием:
   cd D:\pa-temp

2. Скопируй файлы из архива (замени пути на свои):
   copy /Y "ПУТЬ\paper.pdf" .
   copy /Y "ПУТЬ\README.md" .
   copy /Y "ПУТЬ\requirements.txt" .
   copy /Y "ПУТЬ\reviewer_response_experiments.py" .

3. Скопируй результаты экспериментов:
   mkdir reviewer_results
   copy /Y "D:\prescribed-axes\reviewer_results\*.*" reviewer_results\

4. Исправь авторство в скрипте (если ещё не сделал):
   Открой exp2_lewm_state\code\lewm_pusht_experiment.py
   Замени строку "Author: Andrew + Claude (Ядро project)" на "Author: Andrey Lazarev"

5. Проверь что нет других упоминаний Claude:
   findstr /s /i "claude" *.py *.ipynb

6. Закоммить и запушь:
   git add -A
   git commit -m "Paper v7: equal-input control, SIGReg ablation, reviewer fixes"
   git push origin main

=== ЧТО ИЗМЕНИЛОСЬ (v6 → v7) ===

Новое:
- paper.pdf — обновлённая статья v7
- reviewer_response_experiments.py — 5 условий × 3 seeds
- reviewer_results/ — результаты equal-input control + SIGReg ablation
- requirements.txt — зависимости
- README.md — обновлён с новыми результатами

Статья v7 — ключевые изменения:
- Abstract ограничен по scope
- Новая Section 2.5 (архитектура предиктора)
- prescribed_full определён в Method
- Нормализация описана
- SIGReg ablation: реальные числа (1.9×, сохранены в репо)
- НОВАЯ Section 4.4: equal-input control (free_3d 7.6× хуже prescribed)
- Физическая аналогия — оговорка про необратимость
- Falsifiability criterion
- Related Work — BYOL, SimSiam, Barlow Twins, DINO
- Limitations расширены (честные «не проверено»)
- CNN baseline оговорка
- Авторство: "Andrey Lazarev" (без Claude)
